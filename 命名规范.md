# 命名规范

本文档适用于 HIDController 项目的所有 C++ 和 Python 代码，包括公开 API、内部实现和测试代码。

---

## 参考依据
本规范综合了以下社区最佳实践：
| 语言 | 参考规范 | 备注 |
|---|---|---|
| C++ | [Google C++ Style Guide](https://google.github.io/styleguide/cppguide.html)、[C++ Core Guidelines](https://isocpp.github.io/CppCoreGuidelines/)、Boost 命名惯例 | 核心风格与 Google 保持一致 |
| Python | [PEP 8](https://peps.python.org/pep-0008/)、[Google Python Style Guide](https://google.github.io/styleguide/pyguide.html) | 严格遵循 PEP 8 |
| 跨语言 | TensorFlow、PyTorch、gRPC 的 C++/Python 绑定层实践 | 公开 API 保持命名一致 |
> 当各规范之间存在冲突时，优先级：**项目已有代码风格 > 本文档 > 参考规范**。

---

## C++ 项目命名规范

遵循现代 C++（C++11 及之后）社区共识，函数和方法采用 `snake_case` 以与 STL 保持一致，类型采用 `PascalCase`。

| 类别 | 风格 | 示例 | 说明 |
|---|---|---|---|
| 类 / 结构体 / 枚举类型 | `PascalCase` | `ImageProcessor`, `NetworkSession`, `ColorSpace` | 类型名大写开头 |
| 函数 / 成员函数 | `snake_case` | `process_image()`, `get_session_status()` | 与 STL 一致（`std::sort`, `std::find_if`） |
| 自由函数（全局/命名空间） | `snake_case` | `compute_histogram()`, `parse_config()` | 同上 |
| 局部变量 / 函数参数 | `snake_case` | `image_data`, `session_id`, `max_retries` | 清晰、简短 |
| 成员变量（非静态） | `snake_case` 末尾加 `_` | `session_id_`, `cache_` | 与参数区分，避免 `session_id = session_id` 这类 bug。<br>**选择后缀而非前缀的原因**：避免与系统保留标识符（以下划线开头）冲突，且符合 Google Style Guide 等主流规范 |
| 静态成员变量（编译期常量） | `kPascalCase` | `static constexpr int kMaxConnections = 128;` | 编译期常量，使用 `k` 前缀 |
| 静态成员变量（非常量/运行时状态） | `s_` 前缀 + `snake_case_` 后缀 | `static Logger* s_instance_;` | `s_` 前缀明确标识为静态可变状态 |
| 常量 | `kPascalCase` | `kMaxRetries`, `kDefaultTimeout` | 现代 C++ 推荐风格 |
| `constexpr` 变量 | `kPascalCase`（同常量） | `constexpr double kPi = 3.14159;` | 编译期常量 |
| 宏（极少使用） | `UPPER_SNAKE_CASE` | `HID_CONTROLLER_VERSION` | 宏必须全大写，避免命名污染 |
| 命名空间 | `snake_case` | `hid_controller::core`, `hid_controller::io` | 全小写，可嵌套 |
| 模板参数 | `PascalCase` 或 `T` | `typename RequestType`, `class Allocator` | 类型参数大写开头 |
| 枚举值（C++11 枚举类） | `kPascalCase` | `Status::kOk`, `Color::kRed` | 与常量风格一致，避免与类型名混淆 |

> **STL 兼容例外**：`std::io_errc` 等 STL 枚举使用 `snake_case` 风格。本规范中的 `kPascalCase` 风格仅适用于项目自有枚举。若枚举需要与 STL 或第三方库的 API 直接交互（如作为 `std::error_code` 的扩展），可例外使用 `snake_case` 以保持一致。

| 异常类型 | `PascalCase` 加 `Error`/`Exception` 后缀 | `NetworkError`, `ParseException` | 明确异常语义 |
| 构造函数 / 析构函数 | 与类名一致 | `ImageProcessor()`, `~ImageProcessor()` | 自动遵循类名 |
| 回调类型别名 | `PascalCase` 加 `Callback`/`Handler`/`Listener` 后缀 | `using OnDataCallback = std::function<void(const Data&)>;` | 语义清晰 |
| Concept（C++20） | `PascalCase` | `template <typename T> concept Readable = ...;` | 以形容词或名词结尾 |
| 测试宏 / 测试用例名 | 宏用 `UPPER_SNAKE_CASE`，用例名用 `snake_case` | `TEST_F(SessionTest, is_valid)` | 测试用例名风格与项目函数名保持一致；Google Test 支持任意合法标识符 |
| 源文件 / 头文件 | `snake_case.cpp` / `.h` | `image_processor.cpp`, `network_session.h` | GCC/Clang 工具链友好 |
| 测试文件 | `snake_case_test.cpp` | `image_processor_test.cpp` | 约定俗成 |

**补充规则：**

| 规则 | 示例 | 说明 |
|---|---|---|
| 布尔变量/函数使用 `is_`/`has_`/`should_` 前缀 | `is_ready()`, `has_data()` | 避免二义性名称如 `valid()` |
| 禁止 Hungarian notation | ~~`pBuffer`, `iCount`~~ | 类型编码前缀不被允许 |
| Getter 只读访问 | 直接使用属性名，返回 `const T&` | `const std::string& session_id() const` | 与 STL 一致（`std::vector::size()`、`std::string::length()`） |
| Setter 写操作 | `set_` 前缀 | `void set_session_id(const std::string& id)` | 与 Getter 形成对称 |
| 不可拷贝类 | `= delete` 拷贝构造和赋值运算符 | 显式删除 |
| 单例类 | 构造函数设为 `private` | 禁止外部实例化 |
| 公共头文件位置 | `include/<project>/` | 统一使用 `.h` 扩展名 |
| 转移所有权的方法 | `take_` 前缀 | `std::vector<int> take_buffer()` | 暗示调用后对象内部状态被重置/转移 |
| 返回 `T` 值拷贝的 Getter | 直接属性名 | `std::string name() const` | 明确表示返回独立副本 |

### 异常安全与 noexcept
| 场景 | 要求 | 示例 |
|---|---|---|
| 移动构造函数 / 移动赋值 | 标记 `noexcept` | `ImageProcessor(ImageProcessor&& other) noexcept;` |
| `swap` 函数 | 标记 `noexcept` | `friend void swap(ImageProcessor& a, ImageProcessor& b) noexcept;` |
| 析构函数 | 隐式或显式 `noexcept` | 析构函数不应抛出异常 |
| 其他不抛异常的函数 | 按实际情况标记 | 仅在明确不会抛出时使用 `noexcept`，避免滥用 |
**理由**：`noexcept` 移动操作使标准容器（如 `std::vector`）能够安全高效地重新分配内存，避免不必要的拷贝。

---

## Python 项目命名规范

严格遵循 [PEP 8](https://peps.python.org/pep-0008/) 规范。

| 类别 | 风格 | 示例 | 说明 |
|---|---|---|---|
| 模块 / 包 | 简短 `snake_case` | `image_processor.py`, `core/` | 全小写，尽量短 |
| 类（普通） | `PascalCase` | `ImageProcessor`, `NetworkSession` | 大写开头 |
| 类（异常） | `PascalCase` 加 `Error`/`Exception` 后缀 | `ValidationError`, `ConnectionTimeout` | 明确异常类型 |
| 函数 / 方法 | `snake_case` | `process_image()`, `get_session_status()` | 必须全小写 + 下划线 |
| 变量（局部/全局） | `snake_case` | `image_data`, `session_id`, `max_retries` | 全小写 |
| 常量（模块级） | `UPPER_SNAKE_CASE` | `MAX_RETRIES`, `DEFAULT_TIMEOUT` | 全大写，下划线分隔 |
| `@property` 属性 | `snake_case` | `session_id`, `timeout` | 公开属性，不加 `get_` 前缀 |
| `@property` setter | 与属性同名 | `@session_id.setter` | Python 自动关联同名属性 |
| 内部存储变量 | `snake_case` 前导 `_` | `_session_id`, `_timeout` | 约定私有 |
| 私有方法（约定） | 前导单下划线 `_` | `_internal_cache()`, `_update_metrics()` | 表示"内部使用" |
| 名称修饰（防子类冲突） | 双前导下划线 `__`（少用） | `__private_attr` | Python 会做名称改写 |
| 魔法方法 | 双下划线包裹 `__xxx__` | `__init__`, `__len__`, `__enter__` | 仅用于 Python 内置协议 |
| 类型别名 | `PascalCase` | `UserId: TypeAlias = int`（Python 3.10+）<br>`type UserId = int`（Python 3.12+） | 明确类型语义，便于静态检查 |
| 测试文件 / 函数 | `test_` 前缀 | `test_image_processor.py`, `test_process()` | pytest / unittest 惯例 |
| 回调 / 事件处理器 | `on_` 前缀或 `_handler` 后缀 | `on_click()`, `error_handler` | 语义清晰 |

**PEP 8 特别强调：**

| 规则 | 示例 | 说明 |
|---|---|---|
| 禁止 `camelCase` 用于函数/变量 | ~~`processImage()`~~ | 除非与现有库兼容 |
| 缩写词在 `PascalCase` 中保持大写 | 推荐 `HTTPSession`；若项目已有 `HttpSession` 风格可保持一致 | 保持缩写完整性是 PEP 8 建议，但一致性优先于绝对遵守 |
| 单字符变量仅限极短作用域 | `for i in range(10)` | 否则使用完整名称 |
| 类和函数之间空两行 | （类定义后空两行再定义函数） | 提高可读性 |
| 避免关键字冲突 | `class_`（避免与 `class` 冲突） | 末尾加单下划线 |

---

## C++ 和 Python 混合项目命名规范

为使用户在两种语言之间切换时降低学习成本，**公开 API 的名称应完全一致**。

### 对应关系

| 用途 | C++ | Python |
|---|---|---|
| 类 / 类型 | `PascalCase` | `PascalCase` |
| 公开函数 | `snake_case` | `snake_case` |
| 变量 | `snake_case` | `snake_case` |
| 常量 | `kPascalCase` | `UPPER_SNAKE_CASE` |
| 文件名 | `snake_case.cpp` / `.h` | `snake_case.py` |

### 核心 API 对照

| 功能 | C++ 公开 API | Python 公开 API |
|---|---|---|
| 创建会话 | `create_session()` | `create_session()` |
| 处理数据 | `process_data()` | `process_data()` |
| 获取版本 | `get_version()` | `get_version()` |

### 属性对应关系

| 场景 | C++ | Python |
|---|---|---|
| 只读属性 | `const std::string& session_id() const` | `@property def session_id(self)` |
| 读写属性 | `const std::string& session_id() const` + `void set_session_id(const std::string&)` | `@property` + `@session_id.setter` |

### 目录结构命名

| 语言 | 目录 | 示例 |
|---|---|---|
| C++ 头文件 | `include/<project>/` | `include/hid_controller/core.h` |
| C++ 源文件 | `src/` | `src/core.cpp` |
| C++ 头文件与实现文件对应 | 基名相同 | `include/hid_controller/image_processor.h` ↔ `src/image_processor.cpp` |
| Python 包 | `src/<project>/`（推荐）或 `<project>/` | `src/hid_controller/core.py` |
| C++ 测试 | `tests/` | `tests/image_processor_test.cpp` |
| Python 测试 | `tests/` | `tests/test_image_processor.py` |

### 跨语言一致性规则

**适用范围**：以下规则仅适用于**公开 API**（public API）。内部实现（C++ 私有/受保护方法；Python `_` 前缀方法）可各自遵循语言特有规范，无需强制对齐。

> **核心原则**：跨语言 API 名称一致性的优先级高于某种语言内部惯用风格。如果存在分歧（例如 C++ 社区惯用 `execute` 而 Python 惯用 `run`），以**用户可见的公开 API 名称**为准，而非语言内部习惯。

| 序号 | 规则 | C++ 示例 | Python 示例 |
|---|---|---|---|
| 1 | 类型名保持一致 | `ImageProcessor` | `ImageProcessor` |
| 2 | 函数名保持一致（`snake_case`） | `process_image()` | `process_image()` |
| 3 | 相似操作使用相同动词 | 若 C++ 用 `process_image()`，Python 也用 `process_image()` | 同类操作尽量统一；具体动词在 API 设计评审中确定 |
| 4 | 命名空间 / 包对应 | `hid_controller::` | `hid_controller.` |
| 5 | 测试文件对应 | `image_processor_test.cpp` | `test_image_processor.py` |
| 6 | getter/setter 对应 | `const std::string& session_id() const` + `set_session_id()` | `@property session_id` + `@session_id.setter` |
